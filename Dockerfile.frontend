FROM node:22-alpine
WORKDIR /app

# Instala dependências (cache do Docker)
COPY frontend/package.json frontend/package-lock.json ./
RUN npm ci

# Copia o código-fonte
COPY frontend/ .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
